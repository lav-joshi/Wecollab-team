import React from 'react'

export default function UserModal() {
  return (
    <div>
        <button class="button" key={idx} onClick={(e) => setcRoom(val.roomName)}>{val.roomName}</button>
    </div>
  )
}
